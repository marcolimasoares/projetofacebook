import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    buttonFooter: {
        width: 100,
        height: 30,
        justifyContent: 'center',
        alignItems: 'center'
    },

    textButton: {
        fontSize: 12,
        fontWeight: 'bold',
        color: '#555555',
    },

    icon:{
        marginBottom: 5,
        color: '#555555',
    },
});